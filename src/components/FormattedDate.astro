---
import { format } from "date-fns";
import { nb } from "date-fns/locale";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {format(date, "yyyy-MM-dd HH:mm", { locale: nb })}
</time>
