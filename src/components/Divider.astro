---
interface Props {
  variant: "big-blue" | "small-gray";
}

const { variant } = Astro.props;
---

{
  variant === "big-blue" ? (
    <div class="h-1.5 w-full -skew-x-12 bg-blue-500" />
  ) : (
    <hr class="w-24 border-t border-gray-300" />
  )
}
