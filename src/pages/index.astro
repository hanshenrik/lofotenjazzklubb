---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import Time from "../components/Time.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Layout from "../layouts/Layout.astro";
import { formatDate, formatWeekdayDateTime } from "../utils/date";
import "../styles/global.css";
import LogoImage from "../assets/images/lofoten-jazzklubb.png";

const events = (await getCollection("event")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <section class="space-y-16">
    <Image src={LogoImage} alt={SITE_TITLE} class="mt-16 w-full" />
    <ul class="grid grid-cols-1 gap-4 md:grid-cols-2">
      {
        events.map((event) => (
          <li>
            <a href={`/arrangement/${event.id}/`} class="no-hover-effect group">
              {event.data.heroImage && (
                <Image
                  width={720}
                  height={360}
                  src={event.data.heroImage}
                  alt={""}
                  class="w-full"
                />
              )}
              <div class="space-y-2 pt-4 pb-6">
                <div class="grid grid-cols-1 items-center">
                  <h4 class="w-fit py-2 text-4xl group-hover:bg-blue-700 group-hover:text-white md:text-6xl">
                    {event.data.title}
                  </h4>
                  <div class="flex">
                    <div class="flex items-center justify-start py-2 pr-2 group-hover:bg-blue-500 group-hover:text-white">
                      <Icon name="circum:calendar-date" size={32} />
                      <Time
                        date={event.data.startDate}
                        format={
                          event.data.endDate
                            ? formatDate
                            : formatWeekdayDateTime
                        }
                        class="first-letter:uppercase"
                      />

                      {event.data.endDate && (
                        <span>
                          –
                          <Time date={event.data.endDate} format={formatDate} />
                        </span>
                      )}
                    </div>

                    <div class="flex items-center justify-start px-2 py-2 group-hover:bg-blue-400 group-hover:text-white">
                      <Icon name="circum:location-on" size={32} />
                      <span>{event.data.venue}</span>
                    </div>
                  </div>
                </div>

                <p>{event.data.description}</p>
              </div>
            </a>
          </li>
        ))
      }
    </ul>
  </section>
</Layout>
